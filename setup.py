from setuptools import setup, find_packages

setup(
    name='foundation_models_agentic_flow',
    version='0.1',
    author="Shriya Nagrath",
    packages=find_packages(),
    install_requires=[],
)
